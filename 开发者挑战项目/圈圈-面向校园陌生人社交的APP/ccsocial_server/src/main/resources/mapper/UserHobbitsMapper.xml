<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="cn.ccsocial.mapper.UserHobbitsMapper">
    <select id="userHobbitsAdd" statementType="CALLABLE" parameterType="String">
        CALL proc_HobbitsAdd(#{userCcid},#{labelId})
    </select>

    <select id="userHobbitsAdd" statementType="CALLABLE" parameterType="String">
        CALL proc_HobbitsAdd(#{userCcid},#{labelId})
    </select>
</mapper>